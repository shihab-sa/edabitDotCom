// arr[i]  arr[i] এটা মেইন ডাটা গুলো দেয়
// i  i দেয় index সেই ডাটার

//Number.isInteger(arr[i])

// if (arr.length === 0) return "arry is empty";



// 


function getFilename(str) {
    let result = str.split("/");
    console.log(result);
  }
  
  console.log(getFilename("C:/Projects/pil_tests/ascii/edabit.txt"));
